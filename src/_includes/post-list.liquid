<ul>
  {% for post in posts reversed %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      (<time datetime="{{ page.date | isoDate }}">{{ post.date | readableDate }}</time>)
    </li>
  {% endfor %}
</ul>
